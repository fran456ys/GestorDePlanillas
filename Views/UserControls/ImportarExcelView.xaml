<UserControl x:Class="GestorDePlanillas.Views.UserControls.ImportarExcelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestorDePlanillas.Views.UserControls"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:UserControls="clr-namespace:GestorDePlanillas.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1720">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <UserControls:PlaceHolderTextBox Width="800" x:Name="tbImportador" HorizontalAlignment="Left"/>

        <DataGrid x:Name="dgTerneros" Grid.Row="1" Width="1300" HorizontalAlignment="Left"
                  AutoGenerateColumns="True" IsReadOnly="True" Background="White" AutoGeneratingColumn="dgTerneros_AutoGeneratingColumn"
                  HorizontalGridLinesBrush="#B8CFCE" />

        <ui:SimpleStackPanel Grid.Row="1" HorizontalAlignment="Right" Width="300" Margin="35">
            <Button x:Name="btnVisualizar" Style="{StaticResource StackButton}" Content="Visualizar Planilla" Click="btnVisualizar_Click"/>
            <Button Style="{StaticResource StackButton}" Content="Descargar Planillas"/>
            <Button x:Name="btnImprimir" Style="{StaticResource StackButton}" Content="Imprimir Planillas" Click="btnImprimir_Click"/>
        </ui:SimpleStackPanel>
    </Grid>
</UserControl>
